---
title: "stat 1"
format: html
---

```{r}
#| label: setup
library(flextable)
options(
  paged.print = FALSE,
  digits = 3
  )
```

```{r}
1/3
```


```{r}
# Sample size
n <- 5
set.seed(94665)

# Generate data
data.frame(
  i = 1:n,
  x = sample(x = 1:10, size = n) # draw integers
) |> 
  as_flextable()
```

